# Anthropic API — see src/client.ts for model routing
ANTHROPIC_API_KEY=

# Supabase project
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Service role key — server-only (lib/supabaseAdmin.ts). Never expose to
# the client, never prefix with NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=
