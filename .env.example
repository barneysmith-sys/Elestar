# Anthropic API — judgement stages. If unset, reasoners fall back to
# deterministic rules and the UI labels DEMO MODE · SIMULATED REASONING.
ANTHROPIC_API_KEY=

# Supabase Auth (publishable key is public; matches the Next.js wizard).
# Linked project: cgrpbnzxcucekxuvnpjn
NEXT_PUBLIC_SUPABASE_URL=https://cgrpbnzxcucekxuvnpjn.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server aliases (optional). Same URL/key as above. Never put the service
# role on NEXT_PUBLIC_.
SUPABASE_URL=https://cgrpbnzxcucekxuvnpjn.supabase.co
SUPABASE_ANON_KEY=

# Service role key — server-only (lib/supabaseAdmin.ts). Never expose to
# the client, never prefix with NEXT_PUBLIC_. Needed for Circuit writes.
SUPABASE_SERVICE_ROLE_KEY=

# Signed inbound webhook for prove@elestar.ai (POST /api/inbound).
# Send the secret as `x-elestar-webhook-secret` or `Authorization: Bearer`.
# If unset, /api/inbound returns 503 and live mail is not accepted.
INBOUND_WEBHOOK_SECRET=

# Local / eval defaults (leave unset):
#   ELESTAR_ALLOW_SIMULATION=true
#   ELESTAR_REQUIRE_PERSISTENCE=false
#   ELESTAR_LIVE_RESEARCH=false
#
# Production (Vercel):
#   ELESTAR_ALLOW_SIMULATION=false
#   ELESTAR_REQUIRE_PERSISTENCE=true
# Fixture/Agent Lab requests then return 403. Live ingest is prove@elestar.ai.
